import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    mode: 'dark',
    background: {
      default: '#261C2C',
      paper: '#261C2C',
    },
    primary: { main: '#6E85B2' },
    secondary: { main: '#5C527F' },
    divider: '#3E2C41',
  },
});
